# MIDI 预先持有零知识证明展品 PRD

- 文档状态：Draft v0.1
- 日期：2026-08-30
- 项目代号：Music-ZK Exhibit
- 产品类型：可复现的密码学概念展品，不是生产服务

## 1. 摘要

Music-ZK Exhibit 用一个受严格限制的 MIDI 文件、一个公开且确定性的参考合成器，以及零知识证明，展示密码学在音乐创作争议中能够提供哪一种证据：

> 在公开歌曲音频 `S` 发布前，某个创作者身份已经提交了 MIDI 承诺 `C_M`。该身份随后发布了一份有效的零知识证明，表明证明者知道一份满足指定 MIDI Profile 的原始 MIDI 文件 `M` 和随机盐 `r`；`M,r` 能打开该承诺，并且 `M` 经指定版本的 ReferenceSynth 渲染后，对应公开参考音频 `V`。

产品必须在首页、证明结果页和导出的公开证据包中，同时显著展示以下限制：

> 本证明不判断公开歌曲 `S` 与参考音频 `V` 是否相似；不证明 MIDI 的原创性、获得方式、完整 DAW 工程的存在、版权归属或创作者未使用 SUNO 等生成式工具；也不排除创作者在发布前根据生成音频或其他来源扒谱制作 MIDI。`S` 与 `V` 的音乐对应关系由听众自行判断。

页面不得使用“原创已验证”“非 AI 已认证”等会扩大证明含义的表达。推荐结果标题为：

> **结构化音乐材料的预先持有证明有效**

## 2. 背景与问题

生成式音乐降低了直接生成完整音频的门槛，也使“这首作品是否经过真实的结构化创作过程”变得难以讨论。创作者可以公开 DAW 工程、MIDI、分轨或编辑历史来增强可信度，但这样会暴露配器、编曲、混音和制作细节。

本项目探索第三条路径：创作者先为私有 MIDI 留下不可悄悄替换的承诺和时间证据，之后在不公开 MIDI 的情况下，证明该 MIDI 能生成一份公开、可听辨的参考音频。

这不是“原创性判定器”，而是一种范围明确的来源信号。

## 3. 价值判断

### 3.1 为什么 MIDI 证据仍有价值

相较于完整 DAW 工程、分轨、插件配置和混音参数，单份 MIDI 通常暴露的制作秘密较少，却包含音高、起止时间和复调关系等结构化音乐信息。因此，“在歌曲发布前持有能生成相符参考音频的 MIDI”可以增加完全没有结构化音乐材料、只持有最终生成音频者伪造声明的成本。

零知识证明的额外价值在于：仅有哈希和时间戳时，创作者最终必须公开 `M,r` 才能让别人验证；加入零知识证明后，创作者可以让公众验证承诺与参考渲染之间的关系，而不公开 `M,r`。

### 3.2 为什么它不能成为原创认证

MIDI 可以通过创作、改编、转录、购买、合作或从生成音频扒谱获得。密码学只能证明程序中被精确定义的关系，不能判断音乐的创作动机和来源。因此，本项目只能提供“预先持有结构化材料”的证据，不提供“独立原创”“未使用 AI”或“拥有完整工程”的结论。

### 3.3 隐私并非绝对

`M` 和 `r` 默认不公开，服务端也不得接收它们。但公开参考音频 `V` 本身可能让专业人士恢复相当多的旋律、节奏与和弦信息。产品必须告诉创作者：零知识证明隐藏的是原始 MIDI 文件和盐，不是隐藏公开音频已经表达的音乐结构。

创作者签名把承诺和证明发布行为归属于同一假名公钥，但不能证明“某个自然人亲自在自己的脑中知道 MIDI”，也不能排除代为生成证明、共享 MIDI/盐或共享私钥。产品表达的是可验证的公钥行为与 witness 关系，不推断人的心理状态。

## 4. 产品目标

### 4.1 核心目标

1. 用一条真实、可验证、端到端的零知识证明展示正式声明，而不是用 mock 或跳过证明的开发模式冒充结果。
2. 展示 `t0: 承诺`、`t1: 发布 S`、`t2: 发布 V 与证明` 的可审计顺序。
3. 将声明绑定到一个 Ed25519 假名创作者公钥；公钥证明同一控制主体，不证明真实姓名。
4. 让创作者的 MIDI、盐和私钥始终留在本地。
5. 让普通听众能同时播放 `S` 与 `V`，清楚区分“密码学结论”和“听觉判断”。
6. 让技术质疑者能够下载证据包、离线验证，并复现至少一组篡改失败实验。
7. 提供一份通俗材料，解释零知识证明的动机、基本思想和能力边界。

### 4.2 成功定义

本项目成功不等于“建成可运营的平台”，而等于公众能看到并复现以下事实：

- 私有 MIDI 没有上传或公开。
- 真实证明绑定到公开承诺、ReferenceSynth 程序版本和参考音频摘要。
- 任意人可验证证明与公开证据的一致性。
- 修改 MIDI、盐、参考音频、日志回执或程序身份会导致对应验证失败。
- 页面没有把有效证明误译为“原创”或“非 AI”。

## 5. 非目标

- 不自动判断 `S` 与 `V` 的旋律、和弦、节奏或听感相似度。
- 不证明 MIDI 的原创性、获得方式或具体创建时间。
- 不排除先生成音频、再扒谱制作 MIDI。
- 不证明完整 DAW 工程、分轨、配器、插件链或混音过程存在。
- 不支持多个 MIDI、Stem、音频轨或复杂工程。
- 不实现通用 MIDI 或通用软件音源兼容。
- 不作为 DAW 插件。
- 不做账号、实名、版权登记、争议仲裁或平台级反作弊。
- 不做公开运营所需的限流、滥用防护、高可用、备份与密钥托管。
- 不在 v1 证明多个 MIDI 承诺之间经过“合法编辑”。
- 不在 v1 使用区块链；Demo 服务端作为受信任时间戳方。

## 6. 角色

### 6.1 创作者

希望在发布歌曲前留下证据，同时不公开 MIDI 和制作细节。创作者愿意保存原始 MIDI、盐和私钥，并理解修改原始 MIDI 的任何字节都会产生新的承诺。

### 6.2 普通质疑者

不需要理解密码学细节，希望知道“系统到底验证了什么”，能够播放 `S`、`V`，并看到简明的有效/无效结果和限制声明。

### 6.3 技术质疑者

不信任网页上的绿色提示，希望检查源码、guest Image ID、公开日志、签名、证明和音频摘要，并离线重跑验证器。

### 6.4 展示者

在本地启动 Python 服务，按脚本演示一次提交、发布、证明和篡改流程。展示者不是生产平台运营者。

## 7. 产品形态与范围

项目是可本地启动的客户端—服务端展品：

- Python CLI 负责身份初始化、MIDI 预检、秘密包管理、调用本地证明程序和上传公开材料。
- Rust ReferenceSynth/zkVM guest 负责受限 MIDI 解析、整数合成和零知识语句。
- Python Demo 服务端负责时间戳、只追加日志、公开文件和展示页面。
- 网页负责解释声明、显示时间线、播放 `S/V`、展示验证结果和导出公开证据包。
- 仓库提供一份约四句《小星星》规模的合规示例；技术用户可以替换自己的合规 Format 0 MIDI。

默认不部署公网实例，也不允许网页上传任意私有 MIDI。私有输入只由本地 CLI 读取。

## 8. 证据时间线

### 8.1 `t0`：提交 MIDI 承诺

创作者在本地选择并冻结 `original.mid`，生成 32 字节随机盐 `r`，计算 `C_M`，用创作者私钥签署提交请求。服务端记录事件、分配单调递增序号和 UTC 时间，返回服务端签名回执及日志包含证明。

此时公开：创作者公钥、`C_M`、协议版本、服务端时间与日志证据。

此时不公开：MIDI、盐、创作者私钥。

### 8.2 `t1`：发布歌曲 `S`

Demo 服务端托管并公开 `S`，记录 `C_S = Hash(S)`、创作者签名、序号和时间。该步骤模拟一个具有可验证发布时间的公开音乐平台。

### 8.3 `t2`：发布参考音频与证明

创作者本地运行真实 prover。证明程序验证 MIDI Profile，重新计算 `C_M`，运行 ReferenceSynth 并得到 `C_V`。创作者公开 `V`、真实 proof receipt、public journal 和同一身份的签名。

展示页确认 `t0 < t1 < t2`，验证日志、签名、`V` 摘要和零知识证明，然后把 `S/V` 的相似性明确留给听众。

## 9. 信息公开边界

| 数据 | 默认位置 | 是否公开 | 说明 |
|---|---|---:|---|
| 原始 `M` | 创作者秘密包 | 否 | 按原始文件字节承诺 |
| 盐 `r` | 创作者秘密包 | 否 | 丢失后不能打开原承诺 |
| 创作者私钥 | 创作者秘密包 | 否 | 丢失后不能延续同一假名身份 |
| 创作者公钥 | 日志/证据包 | 是 | 不等于实名或版权身份 |
| `C_M` | 日志/证据包 | 是 | 盐用于阻止低熵 MIDI 的离线猜测 |
| 歌曲 `S` 与 `C_S` | 服务端 | 是 | `S/V` 关系不进入证明 |
| 参考音频 `V` 与 `C_V` | 服务端/证据包 | 是 | `V` 可能泄露可听见的音乐结构 |
| proof receipt 与 journal | 服务端/证据包 | 是 | 必须由标准验证器和固定 Image ID 验证 |
| 日志回执、检查点和包含证明 | 服务端/证据包 | 是 | Demo 仍信任服务端不倒签或分叉 |

## 10. 常见展示流程

### 10.1 创作者：正常完成一次声明

1. 初始化一个假名创作者身份。
2. 运行 MIDI 预检；CLI 显示支持范围和预计音频时长。
3. CLI 要求确认：“此版本按原始字节承诺，重新保存或修改文件将产生不同承诺。”
4. 生成秘密包并提交 `C_M`；展示页出现 `t0` 事件。
5. 通过 Demo 服务端发布 `S`；展示页出现 `t1` 事件。
6. 本地生成 `V` 和真实证明；过程中不上传 `M,r`。
7. 发布 `V` 和证明；展示页出现 `t2` 事件。
8. 页面显示“结构化音乐材料的预先持有证明有效”，并紧邻显示能力边界。

### 10.2 创作者：MIDI 在提交后被修改

1. 创作者重新保存或修改 MIDI。
2. CLI 在证明前计算当前文件承诺，发现与 `t0` 的 `C_M` 不符。
3. CLI 停止并显示：必须找回原始字节文件；不能用修改后的文件替代旧承诺。
4. 产品允许为新文件创建新的承诺，但不能覆盖旧日志。

### 10.3 创作者：盐或身份私钥丢失

- 盐丢失：旧承诺无法生成对应证明，也不能由服务端恢复。
- 私钥丢失：仍可验证既有证据，但不能以同一创作者公钥发布后续事件。
- 页面和 CLI 不提供“重置旧承诺”的虚假恢复能力。

### 10.4 创作者：以后自愿公开 MIDI

创作者可额外发布 `M,r`。验证者可以直接重算 `C_M` 和 `V`；原零知识证明仍然有效，但隐私已经由创作者主动放弃。公开不是正常流程的必需步骤。

### 10.5 普通质疑者：快速查看声明

1. 打开声明页，先阅读“一句话证明内容”和“一句话不能证明内容”。
2. 查看 `t0/t1/t2` 时间线及创作者公钥短指纹。
3. 看到密码学验证是否通过。
4. 分别播放 `S` 和 `V`，自行判断旋律、节奏和和弦是否相符。
5. 展开“为什么这不是原创认证”，了解扒谱、AI 辅助与版权边界。

### 10.6 普通质疑者：“证明有效，但我认为两段音频不像”

系统不得反驳或覆盖听众判断。页面只说明：证明确认隐藏 MIDI 能生成当前 `V`，没有确认 `S` 与 `V` 的关系。质疑者可以把争议准确表述为“密码学部分有效，但音乐对应关系存疑”。

### 10.7 普通质疑者：“创作者可能先用 AI 生成，再提前扒谱”

系统明确承认这种可能性属于非目标。有效证明只能反驳“发布前完全没有该结构化 MIDI”这一更窄的说法，不能反驳“该 MIDI 可能来自 AI 音频转录”。

### 10.8 技术质疑者：不信任网页结果

1. 下载公开证据包和服务器公开验证密钥。
2. 从源码重建或核对 ReferenceSynth guest 的 Image ID。
3. 离线验证服务端签名、创作者签名、日志包含证明、事件顺序、`S/V` 摘要和 zkVM receipt。
4. 验证器输出逐项结果，不合并成模糊的单一布尔值。

### 10.9 展示者：篡改实验

展品至少提供以下实验：

1. 正常证据全部通过。
2. 修改原 MIDI 一个字节后，不能打开原 `C_M`。
3. 修改公开 `V` 一个 PCM 采样后，`C_V` 不匹配。
4. 替换盐后，`C_M` 不匹配。
5. 篡改日志回执或事件顺序后，签名、包含证明或顺序检查失败。

## 11. 证明结果页

### 11.1 首屏顺序

1. **结论**：结构化音乐材料的预先持有证明有效/无效/未完成。
2. **密码学已经证明**：用三条以内自然语言描述。
3. **请听众自行判断**：并列的 `S`、`V` 播放器，不默认同步播放。
4. **本系统不能证明**：始终展开，不藏在 tooltip 或页脚。
5. **时间线**：`t0/t1/t2`、序号和签名状态。
6. **技术细节**：公钥、承诺、Image ID、协议版本、日志根和下载链接。

### 11.2 分项状态

- 创作者身份签名：通过/失败
- 承诺事件早于发布事件：通过/失败
- 服务端日志与签名：通过/失败
- ReferenceSynth 程序身份：通过/失败
- 零知识证明：真实证明通过/失败/仅开发执行，不能作为证明
- 公开 `V` 摘要：通过/失败
- `S/V` 音乐相似性：**未由系统判断**

任何关键密码学检查失败时，页面不得显示总体有效。开发模式生成的假 receipt 必须显示红色“不是密码学证明”。

## 12. 功能需求

### P0

- 本地生成并安全保存创作者 Ed25519 密钥。
- 严格预检受限 MIDI Profile。
- 生成 32 字节加密安全随机盐和原始字节承诺。
- 创建提交、歌曲发布和证明发布三类签名事件。
- 服务端生成只追加日志、签名时间回执和包含证明。
- 公开托管 `S`、`V` 和证据包；拒绝上传 `M,r` 字段。
- 使用固定整数 ReferenceSynth 生成规范 WAV。
- 在 zkVM guest 内解析 MIDI、检查 Profile、重新计算承诺并流式计算规范 WAV 摘要。
- 生成并验证真实 RISC Zero receipt；发布构建所对应的 Image ID。
- 提供普通查看页、技术详情页和离线验证命令。
- 提供五种正反向展示实验。

### P1

- 一键演示脚本，按时间线暂停并解释每一步。
- 证明前执行 cycle count 与内存预估。
- 导出公开证据包和私密恢复包清单。
- 从源码重建 Image ID 的说明。

## 13. 验收标准

### 13.1 产品与表述

- 首页、结果页、公开证据包说明均包含正式声明和完整限制声明。
- 页面不存在“原创已验证”“非 AI”或意思等价的误导性徽章。
- 创作者流程与质疑者流程都可以仅依赖仓库内材料完成。
- 普通查看者能在一次页面浏览中区分密码学判断与听觉判断。

### 13.2 密码学与隐私

- 发布证据包不包含 MIDI、盐、私钥、私有 zkVM 输入或它们的临时副本。
- 标准验证器在禁用开发模式时验证真实 receipt 成功。
- 使用错误 Image ID、错误 `C_M`、错误 `C_V` 或被篡改 journal 时验证失败。
- 服务端访问日志和数据库字段不出现 MIDI 或盐。
- 远程 proving 在 v1 被禁止，因为远程 prover 会看到私有输入。

### 13.3 规模与性能

目标工作负载：单个 Format 0 MIDI、最长 60 秒、同时最多 4 音、固定音色。目标指标为：

- 60 秒工作负载证明生成不超过 30 分钟。
- 峰值内存不超过 6 GB。
- receipt 不超过 10 MB。
- 验证不超过 10 秒。

最低实验基线为：30 秒工作负载在 60 分钟内生成真实证明。8 GB 是希望达到的本地 proving 目标，不是未经测量即可宣称的兼容性。RISC Zero 当前文档一般建议本地 proving 使用至少 16 GB，但同时说明可用内存低于 10 GB 时可以降低 segment size limit。因此项目必须先测量，再作如下处理：

- 如果 8 GB 环境通过最低基线，可明确展示测得的机器、版本、峰值内存和耗时。
- 如果仅 16 GB 环境能生成真实证明，展品仍可证明概念成立，但必须把“8 GB proving 未达成”列为公开未满足目标。
- 不得用 dev mode、仅执行不证明或远程证明替代本地真实证明后声称满足验收。

参考：[RISC Zero zkVM Overview](https://dev.risczero.com/api/zkvm/)；[Local Proving](https://dev.risczero.com/api/generating-proofs/local-proving)；[Dev Mode](https://dev.risczero.com/api/generating-proofs/dev-mode)。

## 14. 风险与控制

| 风险 | 影响 | 控制 |
|---|---|---|
| 宣传结论扩大 | 被理解为原创/非 AI 认证 | 固定声明文案；禁止误导徽章；分开展示密码学与听觉判断 |
| 服务端倒签或向不同用户展示不同日志 | 发布时间证据失真 | Demo 明确信任假设；签名检查点和包含证明；未来外部锚定 |
| MIDI 熵低，可猜测承诺 | 隐私泄露 | 32 字节随机盐、域分离、长度前缀；盐不上传 |
| `V` 泄露旋律结构 | 创作者低估隐私影响 | 提交前明确警告；允许先试听再决定是否发布 |
| 原始 MIDI 被编辑器改写 | 无法打开旧承诺 | 按原字节冻结；秘密包保存；证明前再次校验 |
| 盐或私钥丢失 | 无法证明或延续身份 | 一次性秘密包清单和备份警告；不承诺服务端恢复 |
| ReferenceSynth 实现不确定 | 不同平台摘要不一致 | 全整数、冻结波表和参数、共享 Rust core、跨平台 golden vectors |
| zkVM 或 guest 有缺陷 | 错误关系也可能被证明 | 固定版本、Image ID、源码可重建、真实负向测试、安全公告检查 |
| 证明资源超预算 | 普通电脑无法生成 | Phase 0 基准门；流式音频摘要；调整 segment size；公开未达目标 |
| 身份被误解为实名 | 错误归属 | 显示“假名公钥”；不声称实名、版权或唯一作者 |

## 15. 未来方向

1. 把日志根锚定到 GitHub、公共透明日志或区块链。
2. 支持规范化 MIDI 语义承诺，使等价导出不因字节差异失效。
3. 支持多个承诺的哈希链，证明声明属于同一连续序列。
4. 定义私有 MIDI 状态机和允许的编辑操作，再研究零知识状态转换或递归证明。
5. 研究 `S/V` 的公开、可解释音乐相似度辅助工具，但不得与密码学结论混为一谈。
6. 扩展多轨、多个 MIDI、配器、Stem 或工程清单承诺。

其中，哈希链只能证明顺序与归属；只有定义并证明状态转换，才可能表达“后继 MIDI 由前驱 MIDI 经合法操作得到”。

## 16. 开放项

以下内容必须由实现阶段的真实基准决定，而不能在 PRD 中假定：

- 8 GB 环境是否能完成 30 秒真实证明。
- composite 与 succinct receipt 在该工作负载下的时间、内存和文件大小。
- 固定波表与极简包络在 8 kHz 下是否达到“旋律和和弦清晰可辨”的主观底线。
- Windows 是否只通过 WSL2 支持；RISC Zero 官方预构建环境当前以 x86-64 Linux 和 arm64 macOS 为主。
