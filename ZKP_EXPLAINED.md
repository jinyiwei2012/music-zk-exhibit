# 零知识证明：不用交出底牌，也能让别人检查你的声明

## 先说动机

很多证明都要求“把证据交出来”。

例如，一位音乐创作者想证明自己在歌曲发布前已经有 MIDI。最直接的方法，是把 MIDI 和文件创建记录全部公开。别人确实更容易相信了，但旋律细节、音符时值和编曲线索也一并泄露。

另一种做法是只说“请相信我”，这又没有可验证性。

零知识证明试图解决的就是这个两难：

> 能不能让别人确信一件明确的事情是真的，同时不把用于证明它的秘密材料交给别人？

答案是：对许多能够写成明确计算规则的问题，可以。

## 一个不涉及音乐的例子

想象你拿到了一道很大的数独题，并且已经知道答案。你不想把答案表交给别人，但想证明“我确实知道一个满足全部规则的解”。

你和验证者事先同意规则：每行、每列、每个小方格都必须符合数独要求。然后你使用一种特殊的密码学方法，把自己的答案作为秘密输入，生成一份证明。

验证者检查这份证明后，可以确信：

- 某个完整答案确实存在；
- 它通过了双方同意的数独检查程序；
- 生成证明的人掌握了使检查通过的秘密输入。

但验证者不会因此看到每个格子里填了什么。

这个比喻省略了大量密码学细节，但抓住了核心：证明公开的是“检查通过”，不是秘密答案本身。

## 零知识证明的三个角色

### 秘密材料

也叫 witness，可以理解为“让声明成立的底牌”。在本项目中，它是原始 MIDI 文件和随机盐。

### 公开规则

所有人都能检查规则写了什么。在本项目中，规则包括：如何解析受限 MIDI、怎样计算承诺、怎样用固定合成器生成参考音频，以及哪些结果可以公开。

### 证明与验证器

证明是一份由秘密材料和公开规则共同产生的密码学对象。验证器不需要秘密材料，就能检查这份证明是否确实对应指定规则和公开结果。

零知识证明不是把程序运行过程“加密录像”后交出去，也不是让验证者相信某家公司。它把“这段公开程序对某个隐藏输入运行后确实通过”变成任何人都能复验的密码学结论。

## 为什么还需要哈希和盐

### 哈希像文件的短指纹

哈希函数可以把任意文件变成固定长度的摘要。文件哪怕只改一个字节，摘要通常也会完全不同。

创作者在歌曲发布前先提交 MIDI 的哈希，就像提前把一个不可轻易替换的文件指纹登记下来。但普通哈希有两个问题。

第一，如果 MIDI 很简单，别人可能枚举许多可能的 MIDI，比较哈希并猜出原文件。第二，仅凭哈希，别人无法知道它对应的 MIDI 是否真的能产生后来公开的参考音频；除非创作者把 MIDI 公开。

### 盐让猜测变得困难

盐是一串由创作者随机生成的秘密字节。系统把 MIDI 和盐一起计算承诺。即使两个人持有完全相同的 MIDI，只要盐不同，承诺也不同。

只要盐足够随机并且没有泄露，外界就很难靠枚举简单 MIDI 来猜中承诺。

盐不是密码恢复码。盐丢失后，创作者无法为原承诺生成正确证明；服务端也不能帮忙找回。

## 这个音乐展品怎样工作

可以把完整过程理解为三次公开事件。

### 第一次：发布前留下承诺

创作者在自己的电脑上选定一份原始 MIDI，并生成随机盐。电脑算出 MIDI 承诺，只把承诺、创作者公钥和签名发给时间戳服务。

时间戳服务记录：“这个公钥控制者在此时提交了这个承诺。”服务端没有拿到 MIDI 或盐。

### 第二次：公开歌曲

之后，创作者公开歌曲音频 `S`。Demo 服务记录它的发布时间和文件摘要。

### 第三次：公开参考音频和证明

创作者在本地运行 ReferenceSynth。它不是常见的复杂软音源，而是一台很小、完全公开、只用确定性整数运算的合成器。相同 MIDI 在所有合规实现中都必须产生逐字节相同的参考 WAV `V`。

与此同时，零知识证明程序秘密读取 MIDI 和盐，检查：

- 这份 MIDI 符合展品支持的严格格式；
- MIDI 与盐确实对应发布前的承诺；
- 用指定 ReferenceSynth 渲染它，得到的结果确实对应公开的 `V`。

程序只公开承诺、参考音频摘要、创作者公钥、事件编号和协议版本，不公开 MIDI 或盐。

最后，任何人都可以运行验证器，检查证明是不是由指定程序正确产生。

## zkVM 可以理解成什么

本项目使用零知识虚拟机，也就是 zkVM。

可以把它想成一台规则特殊的公开计算机：

- 大家知道这台计算机运行的程序是什么；
- 创作者可以把秘密输入交给自己电脑上的这台虚拟机；
- 虚拟机运行 MIDI 解析和合成程序；
- 最后产生一张可以公开验证的“计算收据”；
- 收据证明指定程序确实成功运行，却不带出秘密输入。

验证者还会核对程序的密码学身份，也就是 Image ID。否则，恶意者可能运行一段更宽松的程序，再声称它是 ReferenceSynth。

## 为什么要有一台很简陋的 ReferenceSynth

真实音乐制作软件包含大量插件、采样库、浮点计算、随机效果和平台差异。同一工程在不同电脑上不一定逐字节产生相同音频，把整套软件放进零知识证明也会非常昂贵。

因此，这个展品故意选择能力很小的合成器：固定采样率、固定整数波表、固定包络、最多四个同时发声的音符。它不追求成品音色，只追求三件事：

- 旋律与和弦可以听辨；
- 所有人都能理解和取得实现；
- 每个平台都得到完全相同的结果。

它生成的 `V` 是一份“结构参考音频”，不是歌曲成品，也不等于 DAW 导出的音频。

## 为什么系统不自动比较歌曲 S 和参考音频 V

正式歌曲可能使用不同音色、速度表达、混音和编码。判断两段音频是否表现了同一旋律与和弦，是另一个很大的音乐信息检索问题，也可能包含主观判断。

本展品故意把职责分开：

- 密码学负责证明隐藏 MIDI、旧承诺和参考音频 `V` 之间的严格关系。
- 听众或音乐专业人士负责判断公开歌曲 `S` 与 `V` 是否相符。

因此，一个完全合理的质疑结论可以是：“零知识证明有效，但我认为 `S` 和 `V` 对不上。”系统不会用密码学结果压过这种音乐判断。

## 从创作者角度看

### 场景一：发布前留下可验证的结构证据

创作者完成了一版旋律与和弦 MIDI，但不想公开音符细节。他先提交承诺，发布歌曲后再公开 `V` 与证明。质疑者可以看到承诺确实更早，也能听到 `V`，但拿不到 MIDI。

### 场景二：发生争议后才决定展示

创作者在发布前已经提交承诺，但当时没有公开参考音频。歌曲发布后出现“完全由生成器直出”的质疑，创作者才在本地生成证明并公开 `V`。只要原 MIDI 和盐仍在，较晚生成证明不会改变较早承诺的时间。

### 场景三：愿意在多年后公开 MIDI

创作者以后可以自愿公开 MIDI 和盐。公众可以直接重算承诺和参考音频。零知识证明让创作者拥有“现在证明、以后再决定是否公开”的选择权。

### 场景四：文件被重新保存

创作者用编辑器重新保存了 MIDI，即使音乐听起来没变，文件字节也可能变化。v1 承诺的是原始文件字节，所以新文件不能打开旧承诺。这个失败不是密码学出错，而是承诺按设计拒绝悄悄替换文件。

## 从普通质疑者角度看

### 质疑一：“网页说成功，我为什么要信？”

质疑者可以下载公开证据包，用独立验证器检查创作者签名、服务端回执、日志顺序、参考音频摘要、程序 Image ID 和零知识证明。网页只是解释层，不是最终信任来源。

### 质疑二：“也许创作者发布前就用 SUNO 生成，再扒成 MIDI。”

完全可能。本证明没有排除这种路线，也不应该声称排除。它只证明在指定时间前已经存在符合规则、能生成 `V` 的结构化 MIDI。

### 质疑三：“证明有效，但 V 跟正式歌曲不像。”

那么应当质疑歌曲对应关系。系统没有证明 `S` 与 `V` 相似，不能用“证明有效”结束这场争论。

### 质疑四：“创作者是不是有完整工程文件？”

证明没有回答。单份受限 MIDI 不包含插件、音色、自动化、分轨或混音信息。更准确的说法是“提前持有结构化音乐材料”，不是“完整工程已验证”。

### 质疑五：“时间戳服务会不会帮创作者倒签？”

Demo 阶段确实信任服务端不会倒签或向不同人展示不同历史。签名和公开日志能发现普通篡改，但不能单独消除服务端作恶。现实系统需要把日志根提交到 GitHub、区块链或多个独立见证者，减少单点信任。

## 证明通过以后，究竟能说什么

可以说：

> 与某个创作者公钥关联的承诺早于歌曲发布事件。一个有效零知识证明表明，存在一份满足受限规范的原始 MIDI 和秘密盐，它们能打开该承诺；该 MIDI 经指定 ReferenceSynth 可生成当前公开参考音频。

不能说：

- 这首歌已经被证明是原创。
- 创作者没有使用 AI 或 SUNO。
- MIDI 是创作者亲手写成的。
- 创作者拥有完整 DAW 工程。
- 创作者拥有版权。
- 正式歌曲与参考音频已经被算法证明相同。

## 零知识证明不是魔法测谎仪

零知识证明非常擅长回答“某个隐藏输入是否通过某段公开程序”这类问题。它不擅长回答没有被写进程序、无法客观定义或依赖现实身份的问题。

一份证明的可靠程度取决于：

- 声明是否写得准确；
- 被证明的程序是否正确；
- 验证者是否核对了正确的程序身份；
- 密码学系统是否安全实现；
- 时间戳方是否满足其信任假设；
- 产品有没有把窄结论夸大成宽结论。

这个展品最重要的教育意义，不只是展示“零知识证明很强”，也展示它的边界：密码学可以让一个精确定义的窄声明变得公开可验证，但不能替人类定义“原创”意味着什么。

## 延伸阅读

- [RISC Zero：zkVM Overview](https://dev.risczero.com/api/zkvm/)
- [RISC Zero：Receipts 101](https://dev.risczero.com/api/zkvm/receipts)
- [RISC Zero：Guest Code 101](https://dev.risczero.com/api/zkvm/guest-code-101)
- [RISC Zero：Local Proving](https://dev.risczero.com/api/generating-proofs/local-proving)

